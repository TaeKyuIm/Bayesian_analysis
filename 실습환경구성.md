# Bayesian_analysis

conda create -n pymc_env -c conda-forge python libpython mkl-service numba python-graphviz scipy arviz
conda activate pymc_env
pip install pymc3

그 이후 g++ 오류가 뜨는데,

c++ 컴파일러가 뜰 경우 경고창에 있는 chain 컴파일러 설치해주고,

conda install mkl mkl-service 해줄 것
